# Server port (Railway often sets PORT automatically)
PORT=3001

# Allow Next.js origin (local or Vercel URL in production)
CORS_ORIGIN=http://localhost:3000

# Shared secret for POST /emit (must match Next.js SOCKET_EMIT_SECRET)
SOCKET_EMIT_SECRET=change-me-local-secret
